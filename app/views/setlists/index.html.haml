%h1 Listing setlists

%table
  %tr
    %th
    %th
    %th
    %th

  - @setlists.each do |setlist|
    %tr
      %td= setlist.id
      %td= link_to 'Show', setlist
      %td= link_to 'Edit', edit_setlist_path(setlist)
      %td= link_to 'Destroy', setlist, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Setlist', new_setlist_path
