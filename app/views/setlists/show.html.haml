%h1 #{@setlist.name} 

- if @setlist.show
  #{show.name} (#{show.date})

%p.help-block Drag songs to reorder

%ol#songs.sortable{'data-setlist_id' => @setlist.id}
  - for song in @setlist.songs
    %li[song]= song.name

%p
  = link_to 'Edit Setlist', edit_resource_path
